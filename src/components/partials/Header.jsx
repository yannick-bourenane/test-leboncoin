import React from "react";
import { Section } from "react-bulma-components";
import Nav from "./Nav";

const Header = () => {
  return (
    <Section>
      <Nav />
    </Section>
  );
};

export default Header;
